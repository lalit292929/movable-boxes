import { EventHandlerDirective } from './event-handler.directive';

describe('EventHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new EventHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
